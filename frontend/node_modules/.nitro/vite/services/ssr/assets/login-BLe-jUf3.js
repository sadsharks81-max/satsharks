import { jsxs, jsx } from "react/jsx-runtime";
import { useNavigate, Link } from "@tanstack/react-router";
import { useState } from "react";
import { u as useAuth, H as Header, b as Input, F as Footer } from "./router-CR3xljxi.js";
import { B as Button } from "./Button-BSYhhpp5.js";
import "@tanstack/react-query";
function Login() {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");
  const {
    login
  } = useAuth();
  const navigate = useNavigate();
  const handleSubmit = async (e) => {
    e.preventDefault();
    setError("");
    if (email && password) {
      const loginError = await login(email, password);
      if (loginError) {
        setError(loginError);
        return;
      }
      navigate({
        to: "/"
      });
    }
  };
  return /* @__PURE__ */ jsxs("div", { className: "min-h-screen flex flex-col bg-background text-on-background animate-fade-up", children: [
    /* @__PURE__ */ jsx(Header, {}),
    /* @__PURE__ */ jsx("main", { className: "flex-1 flex items-center justify-center p-6", children: /* @__PURE__ */ jsxs("div", { className: "w-full max-w-md rounded-2xl bg-surface-container-lowest p-8 shark-shadow border border-outline-variant/40", children: [
      /* @__PURE__ */ jsxs("div", { className: "text-center mb-8", children: [
        /* @__PURE__ */ jsx("h1", { className: "font-display text-3xl font-extrabold tracking-[-0.02em]", children: "Welcome Back" }),
        /* @__PURE__ */ jsx("p", { className: "mt-2 text-sm text-on-surface-variant", children: "Log in to continue your SAT prep journey" })
      ] }),
      /* @__PURE__ */ jsxs("form", { onSubmit: handleSubmit, className: "space-y-5", children: [
        /* @__PURE__ */ jsx(Input, { label: "Email Address", type: "email", value: email, onChange: (e) => setEmail(e.target.value), placeholder: "you@example.com", required: true }),
        /* @__PURE__ */ jsx(Input, { label: "Password", type: "password", value: password, onChange: (e) => setPassword(e.target.value), placeholder: "••••••••", required: true }),
        error && /* @__PURE__ */ jsx("p", { className: "rounded-lg border border-error/30 bg-error/10 px-3 py-2 text-sm font-medium text-error", children: error }),
        /* @__PURE__ */ jsx("div", { className: "flex justify-end", children: /* @__PURE__ */ jsx(Link, { to: "/auth/forgot-password", className: "font-mono text-[12px] uppercase tracking-[0.08em] text-primary hover:text-primary-container transition-colors", children: "Forgot Password?" }) }),
        /* @__PURE__ */ jsx(Button, { type: "submit", className: "w-full", children: "Log In" })
      ] }),
      /* @__PURE__ */ jsxs("p", { className: "mt-6 text-center text-sm text-on-surface-variant", children: [
        "Don't have an account?",
        " ",
        /* @__PURE__ */ jsx(Link, { to: "/auth/register", className: "font-semibold text-primary hover:underline", children: "Register here" })
      ] })
    ] }) }),
    /* @__PURE__ */ jsx(Footer, {})
  ] });
}
export {
  Login as component
};
