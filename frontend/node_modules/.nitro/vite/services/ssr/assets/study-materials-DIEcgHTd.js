import { jsx, jsxs } from "react/jsx-runtime";
import { useState, useEffect } from "react";
import { T as TeacherLayout } from "./TeacherLayout-BcyuUHQe.js";
import { I as Icon, g as getBackendUrl, M as Modal, b as Input, a as api } from "./router-CR3xljxi.js";
import { T as Textarea } from "./Textarea-BWaDv9iL.js";
import { E as EmptyState } from "./EmptyState-Ce3TFmft.js";
import "@tanstack/react-router";
import "@tanstack/react-query";
function TeacherStudyMaterials() {
  const [materials, setMaterials] = useState([]);
  const [loading, setLoading] = useState(true);
  const [modalOpen, setModalOpen] = useState(false);
  const [title, setTitle] = useState("");
  const [description, setDescription] = useState("");
  const [file, setFile] = useState(null);
  const [uploading, setUploading] = useState(false);
  const [error, setError] = useState("");
  const fetchMaterials = async () => {
    try {
      const res = await api.get("/api/study-materials");
      if (res.success) {
        setMaterials(res.materials || []);
      }
    } catch (err) {
      console.error(err);
    } finally {
      setLoading(false);
    }
  };
  useEffect(() => {
    fetchMaterials();
  }, []);
  const handleUpload = async (e) => {
    e.preventDefault();
    if (!title || !file) {
      setError("Title and PDF file are required.");
      return;
    }
    setUploading(true);
    setError("");
    const formData = new FormData();
    formData.append("title", title);
    formData.append("description", description);
    formData.append("file", file);
    try {
      const token = localStorage.getItem("accessToken");
      const res = await fetch(`${getBackendUrl()}/api/study-materials`, {
        method: "POST",
        headers: token ? {
          Authorization: `Bearer ${token}`
        } : {},
        body: formData
      });
      const data = await res.json();
      if (data.success) {
        setModalOpen(false);
        setTitle("");
        setDescription("");
        setFile(null);
        fetchMaterials();
      } else {
        setError(data.error || "Failed to upload study material.");
      }
    } catch (err) {
      setError("Failed to upload file due to a network connection error.");
    } finally {
      setUploading(false);
    }
  };
  const handleDelete = async (id) => {
    if (!confirm("Are you sure you want to delete this study material?")) return;
    try {
      const res = await api.delete(`/api/study-materials/${id}`);
      if (res.success) {
        setMaterials((prev) => prev.filter((m) => m._id !== id));
      } else {
        alert(res.error || "Failed to delete study material.");
      }
    } catch (err) {
      alert("Failed to delete. Connection error.");
    }
  };
  const formatSize = (bytes) => {
    if (bytes === 0) return "0 Bytes";
    const k = 1024;
    const sizes = ["Bytes", "KB", "MB"];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + " " + sizes[i];
  };
  return /* @__PURE__ */ jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("h1", { className: "text-3xl font-bold", children: "Study Materials" }),
        /* @__PURE__ */ jsx("p", { className: "text-on-surface-variant text-sm mt-1", children: "Publish notes, study documents, and sheets for students" })
      ] }),
      /* @__PURE__ */ jsxs("button", { onClick: () => {
        setModalOpen(true);
        setError("");
      }, className: "btn-shimmer inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-on-primary shark-shadow hover:bg-accent transition-all cursor-pointer border-none", children: [
        /* @__PURE__ */ jsx(Icon, { name: "upload_file", className: "text-lg" }),
        " Upload Notes"
      ] })
    ] }),
    loading ? /* @__PURE__ */ jsx("div", { className: "text-center py-12 text-on-surface-variant", children: "Loading materials..." }) : materials.length === 0 ? /* @__PURE__ */ jsx(EmptyState, { icon: "menu_book", title: "No Study Materials Uploaded", description: "Upload cheat sheets, formulas guides, or study notes for your students to read inside the portal." }) : /* @__PURE__ */ jsx("div", { className: "rounded-2xl bg-surface border border-outline-variant/40 overflow-hidden shark-shadow", children: /* @__PURE__ */ jsx("div", { className: "overflow-x-auto", children: /* @__PURE__ */ jsxs("table", { className: "w-full text-left border-collapse text-sm", children: [
      /* @__PURE__ */ jsx("thead", { children: /* @__PURE__ */ jsxs("tr", { className: "bg-surface-container-low border-b border-outline-variant/40 text-xs uppercase tracking-wider text-on-surface-variant", children: [
        /* @__PURE__ */ jsx("th", { className: "p-4 font-bold", children: "Document Title" }),
        /* @__PURE__ */ jsx("th", { className: "p-4 font-bold", children: "File Information" }),
        /* @__PURE__ */ jsx("th", { className: "p-4 font-bold", children: "Date Shared" }),
        /* @__PURE__ */ jsx("th", { className: "p-4 font-bold", children: "Actions" })
      ] }) }),
      /* @__PURE__ */ jsx("tbody", { className: "divide-y divide-outline-variant/20", children: materials.map((mat) => /* @__PURE__ */ jsxs("tr", { className: "hover:bg-surface-container-low/40 transition-colors", children: [
        /* @__PURE__ */ jsxs("td", { className: "p-4", children: [
          /* @__PURE__ */ jsx("div", { className: "font-semibold text-on-surface text-sm", children: mat.title }),
          mat.description && /* @__PURE__ */ jsx("div", { className: "text-xs text-on-surface-variant mt-1 line-clamp-1 max-w-md", children: mat.description })
        ] }),
        /* @__PURE__ */ jsxs("td", { className: "p-4 text-xs font-mono text-on-surface-variant", children: [
          /* @__PURE__ */ jsx("div", { children: mat.fileName }),
          /* @__PURE__ */ jsx("div", { className: "mt-0.5 opacity-80", children: formatSize(mat.fileSize) })
        ] }),
        /* @__PURE__ */ jsx("td", { className: "p-4 text-xs text-on-surface-variant", children: new Date(mat.createdAt).toLocaleDateString() }),
        /* @__PURE__ */ jsx("td", { className: "p-4", children: /* @__PURE__ */ jsxs("div", { className: "flex gap-2", children: [
          /* @__PURE__ */ jsxs("a", { href: mat.fileUrl.startsWith("http") ? mat.fileUrl : `${getBackendUrl()}${mat.fileUrl}`, target: "_blank", rel: "noopener noreferrer", className: "px-3 py-1.5 bg-primary/10 text-primary hover:bg-primary/20 rounded-lg text-xs font-semibold transition-colors flex items-center gap-1", children: [
            /* @__PURE__ */ jsx(Icon, { name: "visibility", className: "text-[14px]" }),
            " View"
          ] }),
          /* @__PURE__ */ jsxs("button", { onClick: () => handleDelete(mat._id), className: "px-3 py-1.5 bg-error/10 text-error hover:bg-error/20 rounded-lg text-xs font-semibold transition-colors flex items-center gap-1 cursor-pointer border-none", children: [
            /* @__PURE__ */ jsx(Icon, { name: "delete", className: "text-[14px]" }),
            " Delete"
          ] })
        ] }) })
      ] }, mat._id)) })
    ] }) }) }),
    /* @__PURE__ */ jsxs(Modal, { open: modalOpen, onClose: () => setModalOpen(false), title: "Upload Study Material", icon: "upload_file", children: [
      error && /* @__PURE__ */ jsxs("div", { className: "mb-4 p-3 bg-error/15 text-error rounded-xl text-sm border border-error/25 flex items-center gap-2", children: [
        /* @__PURE__ */ jsx(Icon, { name: "error", className: "shrink-0" }),
        /* @__PURE__ */ jsx("span", { children: error })
      ] }),
      /* @__PURE__ */ jsxs("form", { onSubmit: handleUpload, className: "space-y-4", children: [
        /* @__PURE__ */ jsx(Input, { label: "Document Title *", value: title, onChange: (e) => setTitle(e.target.value), required: true, placeholder: "e.g. SAT Grammar Rules Cheatsheet" }),
        /* @__PURE__ */ jsx(Textarea, { label: "Short Description", value: description, onChange: (e) => setDescription(e.target.value), rows: 3, placeholder: "Describe what these notes cover..." }),
        /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx("label", { className: "mb-1.5 block font-mono text-[11px] uppercase tracking-[0.08em] text-on-surface-variant font-bold", children: "PDF Note File *" }),
          /* @__PURE__ */ jsxs("div", { className: "border-2 border-dashed border-outline-variant/60 rounded-xl p-6 text-center hover:border-primary/40 transition-colors relative", children: [
            /* @__PURE__ */ jsx(Icon, { name: "cloud_upload", className: "text-3xl text-on-surface-variant/40 mb-1" }),
            /* @__PURE__ */ jsx("p", { className: "text-xs text-on-surface-variant", children: file ? file.name : "Select or drag & drop PDF note file" }),
            /* @__PURE__ */ jsx("p", { className: "text-[10px] text-on-surface-variant/60 mt-1", children: "PDF file format only (up to 20MB)" }),
            /* @__PURE__ */ jsx("input", { type: "file", accept: ".pdf", onChange: (e) => setFile(e.target.files?.[0] || null), className: "absolute inset-0 w-full h-full opacity-0 cursor-pointer" })
          ] })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "flex gap-4 pt-4 border-t border-outline-variant/30", children: [
          /* @__PURE__ */ jsx("button", { type: "button", onClick: () => setModalOpen(false), className: "flex-1 py-2.5 rounded-xl border border-outline-variant hover:bg-surface-container-low text-xs font-bold transition-all cursor-pointer", children: "Cancel" }),
          /* @__PURE__ */ jsx("button", { type: "submit", disabled: uploading, className: "flex-1 py-2.5 rounded-xl bg-primary text-on-primary hover:bg-accent font-semibold transition-all disabled:opacity-50 cursor-pointer shadow-sm text-xs border-none", children: uploading ? "Uploading..." : "Publish Material" })
        ] })
      ] })
    ] })
  ] });
}
const SplitComponent = () => /* @__PURE__ */ jsx(TeacherLayout, { activeItem: "/teacher/study-materials", children: /* @__PURE__ */ jsx(TeacherStudyMaterials, {}) });
export {
  SplitComponent as component
};
