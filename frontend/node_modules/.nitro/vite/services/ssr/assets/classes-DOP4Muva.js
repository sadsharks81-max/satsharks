import { jsxs, jsx, Fragment } from "react/jsx-runtime";
import { useNavigate, Link } from "@tanstack/react-router";
import { T as TeacherLayout } from "./TeacherLayout-BcyuUHQe.js";
import { useState, useEffect } from "react";
import { u as useAuth, I as Icon, M as Modal, b as Input, a as api } from "./router-CR3xljxi.js";
import { B as Badge } from "./Badge-CruYiyAR.js";
import { T as Textarea } from "./Textarea-BWaDv9iL.js";
import "@tanstack/react-query";
function TeacherClasses() {
  const {
    user
  } = useAuth();
  const navigate = useNavigate();
  const [classes, setClasses] = useState([]);
  const [loading, setLoading] = useState(true);
  const [modalOpen, setModalOpen] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  console.log("TeacherClasses Render - User Context:", user);
  const [form, setForm] = useState({
    title: "",
    description: "",
    scheduledAt: "",
    duration: 60
  });
  const fetchClasses = () => {
    setLoading(true);
    api.get("/api/live-classes").then((res) => {
      if (res.success) {
        setClasses(res.classes || []);
      }
      setLoading(false);
    }).catch(() => setLoading(false));
  };
  useEffect(() => {
    fetchClasses();
  }, []);
  const handleCreateClass = async (e) => {
    e.preventDefault();
    if (!form.title || !form.scheduledAt) return;
    setSubmitting(true);
    console.log("Create Class Submission - User Context:", user);
    try {
      const teacherId = user?.id || user?._id || user?.userId;
      console.log("Resolved Teacher ID:", teacherId);
      if (!teacherId) {
        alert("Teacher profile not loaded. Please re-login. (Details: " + JSON.stringify(user) + ")");
        return;
      }
      const res = await api.post("/api/live-classes", {
        ...form,
        teacherId
      });
      if (res.success) {
        fetchClasses();
        setModalOpen(false);
        setForm({
          title: "",
          description: "",
          scheduledAt: "",
          duration: 60
        });
        if (res.liveClass?._id) {
          await api.put(`/api/live-classes/${res.liveClass._id}/status`, {
            status: "LIVE"
          });
          navigate({
            to: "/live-class/$id",
            params: {
              id: res.liveClass._id
            }
          });
        }
      } else {
        alert(res.error || "Failed to create class session.");
      }
    } catch (err) {
      alert("Error creating class session.");
    } finally {
      setSubmitting(false);
    }
  };
  const handleUpdateStatus = async (id, status) => {
    try {
      const res = await api.put(`/api/live-classes/${id}/status`, {
        status
      });
      if (res.success) {
        fetchClasses();
      }
    } catch (err) {
      alert("Failed to update status.");
    }
  };
  return /* @__PURE__ */ jsxs(TeacherLayout, { activeItem: "/teacher/classes", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between mb-8", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("h1", { className: "text-3xl font-display font-bold text-on-background mb-2", children: "My Live Classes" }),
        /* @__PURE__ */ jsx("p", { className: "text-on-surface-variant text-sm", children: "Launch live sessions, schedule upcoming ones, and review completed sessions." })
      ] }),
      /* @__PURE__ */ jsxs("button", { onClick: () => setModalOpen(true), className: "flex items-center gap-2 px-5 py-3 bg-primary text-on-primary rounded-xl font-bold text-sm hover:bg-accent transition-colors cursor-pointer border-none shadow-sm", children: [
        /* @__PURE__ */ jsx(Icon, { name: "add" }),
        "Schedule Class"
      ] })
    ] }),
    loading ? /* @__PURE__ */ jsx("div", { className: "text-center p-12 text-on-surface-variant animate-pulse", children: "Loading classes..." }) : classes.length === 0 ? /* @__PURE__ */ jsxs("div", { className: "bg-surface-container-lowest border border-outline-variant/35 rounded-2xl p-16 text-center flex flex-col items-center", children: [
      /* @__PURE__ */ jsx(Icon, { name: "video_camera_front", className: "text-5xl text-on-surface-variant/40 mb-4" }),
      /* @__PURE__ */ jsx("p", { className: "text-lg font-bold text-on-surface mb-2", children: "No live classes found" }),
      /* @__PURE__ */ jsx("p", { className: "text-sm text-on-surface-variant max-w-sm", children: `You don't have any classes scheduled yet. Click "Schedule Class" above to create your first class.` })
    ] }) : /* @__PURE__ */ jsx("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: classes.map((c) => /* @__PURE__ */ jsxs("div", { className: "bg-surface-container-lowest border border-outline-variant/35 rounded-2xl p-6 shark-shadow flex flex-col justify-between", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between gap-4 mb-3", children: [
          /* @__PURE__ */ jsx(Badge, { variant: c.status === "LIVE" ? "success" : c.status === "COMPLETED" ? "neutral" : c.status === "CANCELLED" ? "error" : "info", children: c.status }),
          /* @__PURE__ */ jsxs("span", { className: "text-[11px] text-on-surface-variant font-mono", children: [
            "ID: ",
            c.roomName
          ] })
        ] }),
        /* @__PURE__ */ jsx("h3", { className: "text-lg font-bold text-on-surface mb-1.5", children: c.title }),
        c.description && /* @__PURE__ */ jsx("p", { className: "text-sm text-on-surface-variant mb-4 line-clamp-2", children: c.description }),
        /* @__PURE__ */ jsxs("div", { className: "space-y-2 border-t border-outline-variant/20 pt-4 mb-6", children: [
          /* @__PURE__ */ jsxs("p", { className: "text-xs text-on-surface-variant flex items-center gap-2", children: [
            /* @__PURE__ */ jsx(Icon, { name: "calendar_today", className: "text-[16px] text-primary" }),
            /* @__PURE__ */ jsx("span", { className: "font-semibold text-on-surface", children: "Scheduled At:" }),
            " ",
            new Date(c.scheduledAt).toLocaleString()
          ] }),
          /* @__PURE__ */ jsxs("p", { className: "text-xs text-on-surface-variant flex items-center gap-2", children: [
            /* @__PURE__ */ jsx(Icon, { name: "schedule", className: "text-[16px] text-primary" }),
            /* @__PURE__ */ jsx("span", { className: "font-semibold text-on-surface", children: "Duration:" }),
            " ",
            c.duration,
            " minutes"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-3", children: [
        c.status === "SCHEDULED" && /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsxs(Link, { to: "/live-class/$id", params: {
            id: c._id
          }, onClick: () => handleUpdateStatus(c._id, "LIVE"), className: "flex-1 flex items-center justify-center gap-2 px-4 py-2.5 bg-primary text-on-primary rounded-xl text-xs font-bold hover:bg-accent transition-colors", children: [
            /* @__PURE__ */ jsx(Icon, { name: "play_arrow" }),
            "Start Class"
          ] }),
          /* @__PURE__ */ jsx("button", { onClick: () => handleUpdateStatus(c._id, "CANCELLED"), className: "px-4 py-2.5 rounded-xl border border-outline-variant/40 hover:bg-surface-container-high text-xs font-bold text-on-surface-variant transition-colors cursor-pointer", children: "Cancel" })
        ] }),
        c.status === "LIVE" && /* @__PURE__ */ jsxs(Fragment, { children: [
          /* @__PURE__ */ jsxs(Link, { to: "/live-class/$id", params: {
            id: c._id
          }, className: "flex-1 flex items-center justify-center gap-2 px-4 py-2.5 bg-success text-white rounded-xl text-xs font-bold hover:opacity-90 transition-opacity", children: [
            /* @__PURE__ */ jsx(Icon, { name: "video_call" }),
            "Join Live Call"
          ] }),
          /* @__PURE__ */ jsx("button", { onClick: () => handleUpdateStatus(c._id, "COMPLETED"), className: "px-4 py-2.5 bg-on-surface-variant/10 text-on-surface-variant hover:bg-on-surface-variant/15 rounded-xl text-xs font-bold transition-colors cursor-pointer border-none", children: "End Class" })
        ] }),
        c.status === "COMPLETED" && /* @__PURE__ */ jsx("div", { className: "w-full text-center text-xs text-on-surface-variant/70 italic py-2", children: "Class session completed" }),
        c.status === "CANCELLED" && /* @__PURE__ */ jsx("div", { className: "w-full text-center text-xs text-error/70 italic py-2", children: "Class session cancelled" })
      ] })
    ] }, c._id)) }),
    /* @__PURE__ */ jsx(Modal, { open: modalOpen, onClose: () => setModalOpen(false), title: "Schedule New Class", children: /* @__PURE__ */ jsxs("form", { onSubmit: handleCreateClass, className: "space-y-4", children: [
      /* @__PURE__ */ jsx(Input, { label: "Class Title", value: form.title, onChange: (e) => setForm({
        ...form,
        title: e.target.value
      }), placeholder: "e.g. SAT Reading: Context Clues", required: true }),
      /* @__PURE__ */ jsx(Textarea, { label: "Description", value: form.description, onChange: (e) => setForm({
        ...form,
        description: e.target.value
      }), placeholder: "Describe what will be covered in this class...", rows: 3 }),
      /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-4", children: [
        /* @__PURE__ */ jsx(Input, { type: "datetime-local", label: "Date & Time", value: form.scheduledAt, onChange: (e) => setForm({
          ...form,
          scheduledAt: e.target.value
        }), required: true }),
        /* @__PURE__ */ jsx(Input, { type: "number", label: "Duration (mins)", value: form.duration, onChange: (e) => setForm({
          ...form,
          duration: parseInt(e.target.value) || 60
        }), required: true })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "flex justify-end gap-3 mt-6", children: [
        /* @__PURE__ */ jsx("button", { type: "button", onClick: () => setModalOpen(false), className: "px-6 py-2.5 rounded-xl border border-outline-variant/40 hover:bg-surface-container-high text-sm font-semibold transition-colors cursor-pointer", children: "Cancel" }),
        /* @__PURE__ */ jsx("button", { type: "submit", disabled: submitting, className: "px-6 py-2.5 rounded-xl bg-primary text-on-primary font-semibold text-sm hover:bg-accent transition-colors disabled:opacity-50 cursor-pointer border-none", children: submitting ? "Scheduling..." : "Schedule" })
      ] })
    ] }) })
  ] });
}
export {
  TeacherClasses as component
};
